<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.Unicon.mapper.memberMapper">
 
<!-- 아이디 중복 체크 -->
    <select id="checkIdOverlap" resultType="int" parameterType="String">
        SELECT COUNT(*) 
        FROM _member 
        WHERE member_id = #{memberId}
    </select>
<!-- 아이디 중복 체크 -->    

<!-- 일반 회원 가입 -->
    <insert id="insertMember" parameterType="com.Unicon.domain.MemberVO">
        insert into _member (member_id, member_pw, member_name, member_nickname, 
                                  member_tel, member_gender, member_birth, member_email,
                                  mail_subscription, road_address, postal_code,
                                  detail_address, extra_address, signup_date)
                                  
        values                   (#{memberId}, #{memberPw}, #{memberName}, #{memberNickname}, 
                                  #{memberTel}, #{memberGender}, #{memberBirth}, #{memberEmail}, 
                                  #{mailSubscription}, #{roadAddress},#{postalCode},#{detailAddress}, #{extraAddress}, now())
    </insert>



</mapper>


